/**
 * 给定一个字符串，找到第一次出现且仅出现两次的字符串，并返回索引，未找到返回 -1
 * @example
 * 'abbc' => 1
 * 'abbbcdde'  => 5
 * 'abc' => -1
 * ' ' => -1
 */

/**
 * @param {string} str
 * @return {number}
 */
export function findFirstTwice(str) {
  // do something
}
