/**
 * 假设有数组 `const arr = [1, 2, 3]`
 *  1. 允许负数索引，arr[-1] >> 3 arr[-3] >> 1
 *  2. 超出返回 `undefined`，arr[-5] >> undefined
 */

/**
 * @param {Array<any>} arr
 */
export default function negativeIndex(arr) {
  // do something
  return;
}
